# Git Workflow Rules

- **Strictly No Auto-Commit / Auto-Push**: Do not run `git commit`, `git push`, or any command that modifies the remote git repository unless the user explicitly asks you to do so.
- All code edits, file creations, and fixes must remain local in the working directory only.
