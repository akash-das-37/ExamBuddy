# Web framework
fastapi[standard]==0.115.6
uvicorn[standard]==0.34.0

# Database
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.1
greenlet==3.1.1

# Settings
pydantic==2.10.4
pydantic-settings==2.7.1

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.1

# Redis (needed for Celery broker in later phases)
redis==5.2.1

# Celery (stub for now, full use in Phase 2)
celery==5.4.0

# Email validation
email-validator==2.2.0

# HTTP client (used by services in later phases)
httpx==0.28.1
